# TODO: This is currently a regular (non-namespace) package because xii-django-river
# is the only distribution published under the `xii.*` prefix. If XII Digital
# publishes additional `xii.*` distributions in the future, remove this
# __init__.py and convert `xii` to a PEP 420 namespace package (and switch
# pyproject.toml to use find_namespace_packages / equivalent discovery).
